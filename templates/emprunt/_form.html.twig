<div class="card">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <p style="margin-top:18px;">
        <button class="btn btn-primary" type="submit">💾 Enregistrer l'emprunt</button>
        <a class="btn btn-outline" href="{{ path('emprunt_index') }}">Annuler</a>
    </p>
{{ form_end(form) }}
</div>
